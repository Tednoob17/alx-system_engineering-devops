
#!/usr/bin/env bash
#I Write a Bash script that configures an Ubuntu server with the below requirements.\
cp /etc/hosts /root/hosts.new
sed -i s/^127.0.0.1/127.0.0.2/ /root/hosts.new && echo -e "\n" && echo " 8.8.8.8 	facebook.com"  >> /root/hosts.new
cp -f /root/hosts.new /etc/hosts
